extern "C" int add(int left, int right) {
    return left + right;
}

extern "C" unsigned rotate_left(unsigned value, unsigned amount) {
    const unsigned bits = sizeof(unsigned) * 8;
    amount %= bits;
    return (value << amount) | (value >> (bits - amount));
}
